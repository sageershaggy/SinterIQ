node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.chrome-debug/
.env*
!.env.example
!.env.production.example
.sinteriq-encryption-key
.sinteriq-auth-secret
.tmp_docx/
data/
*.db
*.db-wal
*.db-shm
*.db-journal
.innovista-encryption-key
.tmp-*/
*.tmp.json
test-results/
playwright-report/

# Ruflo local secrets and runtime data
.env
.env.local
.env.*.local
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/
# Ruflo runtime state: the memory index and policy receipts are rebuilt locally and
# change on every memory write, so committing them would churn the repo.
.claude-flow/policy/
.swarm/hnsw.index
.swarm/hnsw.metadata.json
# Temporary git worktrees Claude Code creates for parallel agents; their work reaches main
# through their branches, never through these folders.
.claude/worktrees/
